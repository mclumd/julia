fif(
    and(hearing(julia, T),
    and(proc(pos_int(now(X)), bound),
    proc(pos_int(saw(person,A,B,C,D,E)), bound))),
    conclusion(ros(raise_arm(B,C), X))).
fif(
    and(hearing(julia, T),
    and(proc(pos_int(now(X)), bound),
    proc(pos_int(saw(person,A,B,C,D,E)), bound))),
    conclusion(ros(speak(), X)) ).
